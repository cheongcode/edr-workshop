# Per-level snapshots

Each folder is the solution as submitted for that level. Later levels include everything from earlier ones.

- [lab1](lab1/) — file hash, C2 block, ransomware, zip, TCP listeners
- [lab2](lab2/) — PID attribution, HTTP DPI, origin tracking, curl
- [lab3](lab3/) — masquerading, backdoor user, WMI persistence, rundll32

The runnable combined agent is [`../edr`](../edr).
